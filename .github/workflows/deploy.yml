name: CI/CD Backend Ventas - EKS

on:
  push:
    branches:
      - deploy

env:
  ECR_REPO: innovatech-backend-ventas
  EKS_NAMESPACE: innovatech
  DEPLOYMENT_NAME: backend-ventas
  CONTAINER_NAME: backend-ventas

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar credenciales AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: us-east-1

<<<<<<< Updated upstream
      - name: Crear .env y docker-compose en EC2 App
        run: |
          COMPOSE_CONTENT=$(cat docker-compose.yml | base64 -w 0)
          aws ssm send-command \
            --instance-ids "i-040e1052a9aa59574" \
            --document-name "AWS-RunShellScript" \
            --parameters commands=["echo $COMPOSE_CONTENT | base64 -d > /home/ssm-user/docker-compose.yml && echo DB_ENDPOINT=${{ secrets.DB_ENDPOINT }} > /home/ssm-user/.env && echo DB_PORT=${{ secrets.DB_PORT }} >> /home/ssm-user/.env && echo DB_NAME=${{ secrets.DB_NAME }} >> /home/ssm-user/.env && echo DB_USERNAME=${{ secrets.DB_USERNAME }} >> /home/ssm-user/.env && echo DB_PASSWORD=${{ secrets.DB_PASSWORD }} >> /home/ssm-user/.env"]

      - name: Ejecutar deploy via SSM en EC2 App
        run: |
          aws ssm send-command \
            --instance-ids "i-040e1052a9aa59574" \
            --document-name "AWS-RunShellScript" \
            --parameters commands='["cd /home/ssm-user && docker-compose pull backend-ventas && docker-compose up -d --no-deps backend-ventas"]'      
=======
      - name: Login a Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Definir tag de imagen
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Build y push imagen
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          docker build -t $ECR_REGISTRY/${ECR_REPO}:${IMAGE_TAG} .
          docker push $ECR_REGISTRY/${ECR_REPO}:${IMAGE_TAG}

      - name: Instalar kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.31.0

      - name: Configurar kubeconfig para EKS
        run: |
          aws eks update-kubeconfig --region us-east-1 --name ${{ secrets.EKS_CLUSTER_NAME }}
          kubectl config current-context

      - name: Aplicar manifests base
        run: |
          kubectl apply -f k8s/namespace.yaml
          kubectl apply -f k8s/storage-class.yaml
          kubectl apply -f k8s/

      - name: Actualizar imagen del deployment
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          kubectl set image deployment/${DEPLOYMENT_NAME} \
            ${CONTAINER_NAME}=$ECR_REGISTRY/${ECR_REPO}:${IMAGE_TAG} \
            -n ${EKS_NAMESPACE}
          kubectl rollout status deployment/${DEPLOYMENT_NAME} -n ${EKS_NAMESPACE}

      - name: Ver estado final
        run: |
          kubectl get pods -n ${EKS_NAMESPACE}
          kubectl get svc -n ${EKS_NAMESPACE}
>>>>>>> Stashed changes
